[project]
name = "internship-backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "asyncpg>=0.31.0",
    "fastapi[standard]>=0.136.3",
    "httpx2>=2.3.0",
    "pwdlib[argon2]>=0.3.0",
    "redis>=8.0.0",
    "sqlalchemy[asyncio]>=2.0.50",
]

[dependency-groups]
dev = [
    "alembic>=1.18.4",
    "mypy>=2.1.0",
    "pre-commit>=4.6.0",
    "pytest>=9.0.3",
    "pytest-asyncio>=1.0.0",
    "ruff>=0.15.15",
]

[tool.pytest.ini_options]
testpaths = ["app/tests"]
pythonpath = ["."]
asyncio_mode = "auto"

[tool.ruff]
line-length = 120
target-version = "py312"
exclude = [".venv","migrations", "alembic"]

[tool.ruff.lint]
select = ["ALL"]
ignore = ["B905", "D100", "D101", "D102", "D103", "D104", "D107", "COM812", "FAST001", "TRY003", "EM101", "FBT001", "FBT002", "TRY301", "D203", "D213", "PLR0913", "DTZ007", "S105"]

[tool.ruff.lint.per-file-ignores]
"app/tests/**/*.py" = ["S101", "TCH001", "TCH002", "TC006"]

[tool.ruff.format]
quote-style = "double"

[tool.mypy]
python_version = "3.12"
strict = true

[[tool.mypy.overrides]]
module = "app.config"
disable_error_code = ["call-arg", "prop-decorator"]
